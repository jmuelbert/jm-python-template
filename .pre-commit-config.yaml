---
# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
exclude: (?x)(
         LICENSE|
         LICENSES/|
         pnpm-lock.yaml)
ci:
  autofix_commit_msg: 🎨 [pre-commit.ci] Auto format from pre-commit.com hooks
  autoupdate_commit_msg: ⬆ [pre-commit.ci] pre-commit autoupdate
fail_fast: false

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        args: [--markdown-linebreak-ext=md]
      - id: end-of-file-fixer
      - id: check-executables-have-shebangs
      - id: check-shebang-scripts-are-executable
      - id: check-yaml
        args: [--allow-multiple-documents]
        exclude: mkdocs.yml
      - id: check-toml
      - id: check-added-large-files
        args: [--maxkb=500]
      - id: check-json
        exclude: (\\.devcontainer/devcontainer\\.json|\\.vscode/.*)
      - id: check-merge-conflict
      - id: check-case-conflict
      - id: detect-private-key
      - id: debug-statements
      - id: check-builtin-literals

  # ------------------------
  # Git / commit checks
  # ------------------------
  - repo: https://github.com/mxr/sync-pre-commit-deps
    rev: v0.0.3
    hooks:
      - id: sync-pre-commit-deps

  - repo: https://github.com/jorisroovers/gitlint
    rev: v0.19.1
    hooks:
      - id: gitlint
        args: [--contrib=CT1, --msg-filename]

  - repo: https://github.com/jumanjihouse/pre-commit-hooks
    rev: 3.0.0
    hooks:
      - id: git-check # Configure in .gitattributes
      - id: git-dirty # Configure in .gitignore
      - id: script-must-not-have-extension
      - id: shellcheck
      - id: shfmt

  # ------------------------
  # Local hooks using package.json scripts
  # ------------------------
  - repo: local
    hooks:
      - id: prettier
        name: Prettier (via pnpm)
        entry: pnpm run format:prettier
        language: system
        types: [json, yaml, markdown]

      - id: taplo
        name: Taplo (TOML format)
        entry: pnpm run format:toml
        language: system
        types: [toml]

      - id: markdownlint
        name: Markdownlint (via pnpm)
        entry: pnpm run format:markdown
        language: system
        types: [markdown, mdx, asciidoc]

      - id: markdown-link-check
        name: Markdown links check (via pnpm)
        entry: pnpm run lint:links
        language: system
        types: [markdown, mdx, asciidoc]

      - id: cspell
        name: Spellcheck (via pnpm)
        entry: pnpm run lint:spell
        language: system
        types: [markdown, mdx, asciidoc, python, text]

  # ------------------------
  # Python linting
  # ------------------------
  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.12.11
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  # ------------------------
  # YAML linting
  # ------------------------
  - repo: https://github.com/adrienverge/yamllint.git
    rev: v1.37.1
    hooks:
      - id: yamllint
        name: yamllint (general yaml)
        args: [-c, .yamllint]
        files: \.(yaml|yml)$
        exclude: ^\.github/workflows/

      # ------------------------
      # YAML linting for GitHub workflows
      # ------------------------
      - id: yamllint
        name: yamllint (github workflows)
        args: [-c, .github/.yamllint]
        files: (^\.github/workflows/.*\.(ya?ml)$|^\.github/actions/.*\.(ya?ml)$)

  # ------------------------
  # GitHub Actions checks
  # ------------------------
  - repo: https://github.com/rhysd/actionlint
    rev: v1.7.1
    hooks:
      - id: actionlint
        name: actionlint (github workflows)
        files: (^\.github/workflows/.*\.(ya?ml)$|^\.github/actions/.*\.(ya?ml)$)

  # ------------------------
  # GitHub JSON & project validation
  # ------------------------
  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.33.3
    hooks:
      - id: check-dependabot
        name: Check Dependabot configuration
        description: Validates .github/dependabot.yml.
        args: [--verbose]
        # The `files` regex ensures the hook only runs on this specific file.
        files: ^\.github/dependabot\.yml$
      - id: check-github-workflows
        name: Check GitHub Workflows
        description: Validates all yaml files in .github/workflows.
        args: [--verbose]
        # The `files` regex targets all YAML files
        #  within the workflows directory.
        files: ^\.github/workflows/.*\.ya?ml$

      - id: check-github-actions
        name: Check GitHub Actions configuration
        description: Validates the action.yml file for a custom action.
        args: [--verbose]
        # This hook targets action definition files, which
        # are often in a subdirectory.
        files: ^(\.github/actions/.*|action)\.ya?ml$

  - repo: https://github.com/amperser/proselint
    rev: 0.14.0 # Check for the latest release on GitHub
    hooks:
      - id: proselint
        # Or any other file extensions you use for prose
        files: \.(md|rst|txt)$

  # ------------------------
  # pyproject.toml validation
  # ------------------------
  - repo: https://github.com/abravalheri/validate-pyproject
    rev: v0.24.1
    hooks:
      - id: validate-pyproject
        name: Validate pyproject.toml

  # ------------------------
  # Pre-commit meta checks
  # ------------------------
  - repo: meta
    hooks:
      - id: check-hooks-apply
      - id: check-useless-excludes
